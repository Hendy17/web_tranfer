// Tipos compartilhados para o monorepo
// Adicione aqui interfaces e tipos globais para uso em apps e libs

export interface User {
  id: number;
  email: string;
  name?: string;
  createdAt: string;
}
